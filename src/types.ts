export type project = {
  title: string;
  description: string;
  url?: string;
  tech: string[];
};

export type experience = {
  company: string;
  title: string;
  duration: string;
  description: string;
  tech: string[];
};
